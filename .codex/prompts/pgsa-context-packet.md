# PGSA Context Packet Prompt

You are working in a PGSA Harness project.

Use this order:

1. Read `pgsa/project.yaml`.
2. Read `pgsa/sessions.yaml`.
3. Read `pgsa/harness/<session>.md`.
4. Read relevant contracts in `pgsa/contracts/`.
5. Read the current session summary in `pgsa/state/`.
6. Check open merge proposals.
7. After the task, update the relevant PGSA artifacts and ledger.

Do not resolve project-level semantic drift with a prose-only handoff. Use a
contract update, review gate, integration blocker, or merge proposal.

