# Claude Code Adapter Commands

Use root `commands/` prompts as the shared source for command-style workflows.

Each command should read `pgsa/harness/<session>.md`, update relevant PGSA
artifacts, and preserve the claim boundary.

