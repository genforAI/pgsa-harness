# Claude Code Adapter Hooks

This folder documents possible Claude Code hook integration points.

No hook is auto-enabled in v0.1. Copy or adapt the root `hooks/*.example.sh`
files into a local Claude Code configuration only if the user explicitly wants
that workflow.

