# PGSA Session Complete Prompt

Before ending the session, verify:

- session summary was updated
- affected contracts were updated
- ledger event was written
- merge proposal exists if global conflict remains
- review or integration artifacts were updated if needed

If unresolved drift remains, generate a continuation prompt instead of claiming full convergence.

