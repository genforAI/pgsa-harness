# PGSA Core Prompts

Core prompt templates that are tool-agnostic should live here.

Tool-specific prompts live in adapter folders when they are implemented, for example:

- `adapters/codex/prompts/`

V0.1 only ships concrete Codex workflow prompt scaffolds. Other adapters are
documented at the adapter root without empty prompt/script placeholder folders.
