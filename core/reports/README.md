# PGSA Reports

Generated report outputs are written into repo-local `pgsa/reports/` folders.

Current generated reports:

- `drift_report.json`

Report generation logic currently lives in `core/metrics/`.
