# Evidence Archive Note

The public v0.1 release tree intentionally excludes old historical review packets from the main source tree.

Current release evidence is kept in:

```text
evidence/local-generated-git-worktree/
```

That evidence supports only this local architecture-protocol claim:

```text
in generated git-worktree suites, PGSA-gated artifacts surfaced semantic contract drift as review / merge blockers without over-blocking the no-drift controls.
```

Historical review packets can be provided separately for audit continuity, but they are not required for normal installation or use of PGSA Harness Core v0.1.

