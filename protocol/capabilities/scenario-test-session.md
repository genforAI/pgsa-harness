# Scenario Test Session

Scenario tests turn acceptance criteria into executable or inspectable evidence. They are especially useful when code agents change frontend behavior, product flows, documentation surfaces, or API contracts.

Reference artifact: `pgsa/scenarios/*.yaml`.

Scenario results should be linked from `integration_report.json` and the coherence ledger.
