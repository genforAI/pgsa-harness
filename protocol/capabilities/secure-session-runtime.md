# Secure Session Runtime

A PGSA session is not only a chat role. In advanced mode it can be treated as a bounded project actor with a runtime profile.

A runtime profile declares allowed reads, writes, commands, tools, denied paths, approval-required paths, network posture, and evidence outputs.

Reference artifact: `pgsa/runtime/session_runtime_profile.yaml`.
