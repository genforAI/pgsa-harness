# Signed Skill Provenance

Agent skills, prompts, role rules, and repo rules act like executable natural-language instructions. PGSA tracks them as signed project artifacts instead of invisible prompt state.

A signed skill entry records skill id, version, signer, status, applicable sessions, dependencies, and risk level. It proves provenance, not inherent safety.

Reference artifact: `pgsa/skills/signed_skill_manifest.yaml`.
