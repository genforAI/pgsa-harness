[project]
name = "pgsa-harness"
version = "0.1.0"
description = "Repo-local project-coherence harness for coding-agent workflows"
requires-python = ">=3.10"
authors = [{name = "PGSA Harness"}]

[project.scripts]
pgsa = "pgsa_cli.main:main"
